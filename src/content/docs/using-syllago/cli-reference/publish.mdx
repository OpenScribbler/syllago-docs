---
title: "syllago publish"
description: "Contribute library content to a registry"
---

## Synopsis

```
syllago publish <name> --registry <registry-name>
```

## Description

Copies a library item to a registry clone, stages the change, and
optionally creates a branch and PR.

Content from private registries is subject to the [registry privacy gate](/syllago-docs/advanced/registry-privacy/) — syllago blocks publishing private-tainted content to public registries.

Examples:
```sh
# Publish a skill to a registry
syllago publish my-skill --registry my-registry

# Publish a specific rule type
syllago publish my-rule --registry team-rules --type rules

# Non-interactive mode
syllago publish my-skill --registry my-registry --no-input
```

## Options

| Flag | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| `--registry` | `string` | — | Yes | Registry name to publish to |
| `--type` | `string` | — | No | Disambiguate when name exists in multiple types |
| `--no-input` | `bool` | — | No | Skip interactive git prompts, stage only |

## Global Options

| Flag | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| `--json` | `bool` | — | No | Output in JSON format |
| `--no-color` | `bool` | — | No | Disable color output |
| `-q`, `--quiet` | `bool` | — | No | Suppress non-essential output |
| `-v`, `--verbose` | `bool` | — | No | Verbose output |

## See Also

- [`syllago share`](/syllago-docs/using-syllago/cli-reference/share/) — Contribute to a team repo instead of a registry
- [`syllago registry`](/syllago-docs/using-syllago/cli-reference/registry/) — Manage registries

---

<small>[Source](https://github.com/OpenScribbler/syllago/blob/main/cli/cmd/syllago/publish_cmd.go)</small>
