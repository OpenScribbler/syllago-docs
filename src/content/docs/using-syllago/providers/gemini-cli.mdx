---
title: Gemini CLI
description: How syllago works with Gemini CLI — supported content types, file locations, hook events, MCP configuration, and format details.
---

{/* AUTO-GENERATED — do not edit. Source: providers.json via sync-providers.ts */}

import ProviderExtensions from '../../../../components/ProviderExtensions.astro';

## Provider Details

| Detail | Value |
|--------|-------|
| **Slug** | `gemini-cli` |
| **Config directory** | `~/.gemini` |
| **Supported content types** | Agents, Commands, Hooks, MCP Configs, Rules, Skills |
| **Emit path** | `GEMINI.md` |

## Supported Content Types

| Content Type | Supported | Install Method |
|-------------|-----------|---------------|
| Rules | Yes | Symlink |
| Skills | Yes | Symlink |
| Agents | Yes | Symlink |
| MCP Configs | Yes | JSON merge |
| Hooks | Yes | JSON merge |
| Commands | Yes | Symlink |
| Loadouts | No | — |

## File Format and Location

| Content Type | Discovery Paths | Global Location | Format |
|-------------|----------------|----------------|--------|
| Rules | `GEMINI.md` | `~/.gemini` | Markdown |
| Skills | `.gemini/skills`, `.agents/skills` | `~/.gemini/skills` | Markdown |
| Agents | `.gemini/agents` | `~/.gemini/agents` | Markdown |
| MCP Configs | `.gemini/settings.json` | — | JSON |
| Hooks | `.gemini/settings.json` | — | JSON |
| Commands | `.gemini/commands` | `~/.gemini/commands` | Markdown |

## Hook Events

Gemini CLI supports **11 hook events** with handler types: `command`.

Hooks are configured in `.gemini/settings.json`.

| Event | Native Name | Category |
|-------|------------|----------|
| `after_model` | `AfterModel` | Model |
| `after_tool_execute` | `AfterTool` | Tool |
| `agent_stop` | `AfterAgent` | Lifecycle |
| `before_compact` | `PreCompress` | Context |
| `before_model` | `BeforeModel` | Model |
| `before_prompt` | `BeforeAgent` | Lifecycle |
| `before_tool_execute` | `BeforeTool` | Tool |
| `before_tool_selection` | `BeforeToolSelection` | Model |
| `notification` | `Notification` | Output |
| `session_end` | `SessionEnd` | Lifecycle |
| `session_start` | `SessionStart` | Lifecycle |

## MCP Configuration

| Detail | Value |
|--------|-------|
| **Transports** | `stdio`, `sse` |
| **Config file** | `.gemini/settings.json` |

## Skills Extensions

Provider-specific skills behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="gemini-cli" contentType="skills" />

## Rules Extensions

Provider-specific rules behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="gemini-cli" contentType="rules" />

## Commands Extensions

Provider-specific commands behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="gemini-cli" contentType="commands" />

## Hooks Extensions

Provider-specific hooks behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="gemini-cli" contentType="hooks" />

## MCP Configs Extensions

Provider-specific mcp configs behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="gemini-cli" contentType="mcp" />


## Detection

Syllago detects Gemini CLI by checking for the `~/.gemini` directory.

## Working with Gemini CLI

```bash
# Add content from Gemini CLI
syllago add --from gemini-cli

# Install content to Gemini CLI
syllago install my-rule --to gemini-cli
```

## See Also

- [Providers Overview](/using-syllago/providers/)
- [Format Conversion](/using-syllago/format-conversion/)

*Generated from syllago dev on 2026-04-13.*
