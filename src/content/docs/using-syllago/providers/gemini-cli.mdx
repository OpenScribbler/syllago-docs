---
title: Gemini CLI
description: How syllago works with Gemini CLI — supported content types, file locations, hook events, MCP configuration, and format details.
---

{/* AUTO-GENERATED — do not edit. Source: providers.json via sync-providers.ts */}

## Provider Details

| Detail | Value |
|--------|-------|
| **Slug** | `gemini-cli` |
| **Config directory** | `~/.gemini` |
| **Supported content types** | [Skills](#skills-conventions), [Hooks](#hook-conventions), [Rules](#rule-conventions), [MCP Configs](#mcp-config-conventions), [Commands](#command-conventions), [Agents](#agent-conventions) |
| **Emit path** | `GEMINI.md` |

## Supported Content Types

| Content Type | Syllago Install Method | Symlink |
|--------------|------------------------|:-------:|
| [Skills](#skills-conventions) | Symlink | Yes |
| [Hooks](#hook-conventions) | JSON merge | No |
| [Rules](#rule-conventions) | Symlink | Yes |
| [MCP Configs](#mcp-config-conventions) | JSON merge | No |
| [Commands](#command-conventions) | Symlink | Yes |
| [Agents](#agent-conventions) | Symlink | Yes |

## Skills Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.gemini/skills`, `.agents/skills`
- **Global install path:** `~/.gemini/skills`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Native frontmatter fields:** `name`, `description`

### Mappings to Canonical

How Gemini CLI's native skills features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`canonical_filename`](/reference/canonical-keys/canonical-filename/) | SKILL.md (required, fixed name) |
| [`description`](/reference/canonical-keys/description/) | yaml frontmatter key: description (required); documents what the skill does and when Gemini should use it — drives auto-invocation decisions |
| [`display_name`](/reference/canonical-keys/display-name/) | yaml frontmatter key: name (required); value should match the skill's directory name |
| [`global_scope`](/reference/canonical-keys/global-scope/) | ~/.gemini/skills/&lt;name&gt;/SKILL.md |
| [`project_scope`](/reference/canonical-keys/project-scope/) | .gemini/skills/&lt;name&gt;/SKILL.md |

### Gemini CLI-specific Skills

These are Gemini CLI-specific skills behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/creating-skills.md" target="_blank" rel="noopener noreferrer">Supporting file directories</a></dt>
    <dd class="provider-extensions__description">Skills may include optional subdirectories alongside SKILL.md for organizing supporting resources: scripts/ for executable scripts, references/ for static documentation, and assets/ for templates and other resources. The skill-creator built-in generates these three directories automatically. Only SKILL.md is required.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/creating-skills.md" target="_blank" rel="noopener noreferrer">Built-in skill-creator skill</a></dt>
    <dd class="provider-extensions__description">Gemini CLI ships a built-in skill named skill-creator that generates new skill directories from a natural language prompt (e.g. "create a new skill called code-reviewer"). It creates the skill directory, SKILL.md with required frontmatter, and the standard scripts/, references/, and assets/ subdirectories.</dd>
  </div>
</dl>

## Hook Conventions

### Native Format

- **File format:** JSON
- **Discovery paths:** `.gemini/settings.json`
- **Syllago install method:** JSON merge
- **Symlink support:** No
- **Config file:** `.gemini/settings.json`
- **Handler types:** `command`
- **Hook events:** 11

| Canonical Event | Native Name | Category |
|-----------------|-------------|----------|
| `after_model` | `AfterModel` | Model |
| `after_tool_execute` | `AfterTool` | Tool |
| `agent_stop` | `AfterAgent` | Lifecycle |
| `before_compact` | `PreCompress` | Context |
| `before_model` | `BeforeModel` | Model |
| `before_prompt` | `BeforeAgent` | Lifecycle |
| `before_tool_execute` | `BeforeTool` | Tool |
| `before_tool_selection` | `BeforeToolSelection` | Model |
| `notification` | `Notification` | Output |
| `session_end` | `SessionEnd` | Lifecycle |
| `session_start` | `SessionStart` | Lifecycle |

### Mappings to Canonical

How Gemini CLI's native hooks features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`decision_control`](/reference/canonical-keys/decision-control/) | exit_code_semantics: Gemini CLI uses exit codes to signal block (non-zero) or allow (zero) decisions; no modify sub-capability documented |
| [`json_io_protocol`](/reference/canonical-keys/json-io-protocol/) | hook_io_protocol: Gemini CLI hooks receive event data as JSON on stdin and return structured JSON responses on stdout |
| [`matcher_patterns`](/reference/canonical-keys/matcher-patterns/) | hook_matchers: Gemini CLI hooks support event and tool name matching to filter when hooks fire |

### Gemini CLI-specific Hooks

These are Gemini CLI-specific hooks behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/hooks/reference.md" target="_blank" rel="noopener noreferrer">Hook event system</a></dt>
    <dd class="provider-extensions__description">Gemini CLI supports 11 hook events covering the full agent lifecycle: BeforeTool, AfterTool, BeforeAgent, AfterAgent, BeforeModel, AfterModel, BeforeToolSelection, SessionStart, SessionEnd, PreCompress, and Notification. Hooks are shell commands configured per-event in settings.json under the hooks key. Each event entry is an array of hook definition objects with optional matcher, optional sequential flag, and a hooks array of command configs.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/hooks/reference.md" target="_blank" rel="noopener noreferrer">stdin/stdout JSON I/O protocol</a></dt>
    <dd class="provider-extensions__description">Hooks communicate via stdin (JSON input) and stdout (JSON output). All hooks receive a common base input with session_id, transcript_path, cwd, hook_event_name, and timestamp. Output fields include decision (allow/deny/block), reason, systemMessage, suppressOutput, continue, and stopReason. Stdout must contain only the JSON object — any plain text pollutes the parse. Stderr is safe for debug logging and is never parsed.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/hooks/reference.md" target="_blank" rel="noopener noreferrer">Exit code semantics</a></dt>
    <dd class="provider-extensions__description">Exit code 0 means success — stdout is parsed as JSON. Exit code 2 means system block — the target action is aborted and stderr is used as the rejection reason. Any other exit code is a non-fatal warning — the interaction continues with original parameters. This three-tier exit code contract is specific to Gemini CLI's hook system.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/hooks/index.md" target="_blank" rel="noopener noreferrer">Matcher-based hook filtering</a></dt>
    <dd class="provider-extensions__description">Hook definitions support a matcher field to filter which tools or triggers fire the hook. For BeforeTool and AfterTool, matchers are regular expressions matched against the tool name (e.g., "write_.*"). For lifecycle events, matchers are exact strings (e.g., "startup"). An empty string or "*" matches all occurrences.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/hooks/index.md" target="_blank" rel="noopener noreferrer">Project hook fingerprinting</a></dt>
    <dd class="provider-extensions__description">Project-level hooks (.gemini/settings.json) are fingerprinted by Gemini CLI. If a hook's name or command changes (e.g., via git pull), it is treated as a new untrusted hook and the user is warned before execution. This is a security measure for untrusted project contexts.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/schemas/settings.schema.json" target="_blank" rel="noopener noreferrer">Sequential execution flag</a></dt>
    <dd class="provider-extensions__description">Hook definition groups support a sequential boolean field. When true, hooks in the group run one after another in order. When false (or omitted), hooks run in parallel. This allows dependency ordering within a hook event group.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/hooks/reference.md" target="_blank" rel="noopener noreferrer">Event-specific hook output fields</a></dt>
    <dd class="provider-extensions__description">Several events support hookSpecificOutput fields beyond the common output schema: BeforeTool supports tool_input (merges with and overrides model arguments); AfterTool supports additionalContext (appended to tool result) and tailToolCallRequest (routes execution to another tool, replacing the original response); AfterAgent supports clearContext (clears LLM conversation history while preserving UI display); BeforeModel supports llm_request (overrides outgoing request) and llm_response (provides synthetic response, skipping the LLM call entirely); BeforeToolSelection supports toolConfig with mode (AUTO/ANY/NONE) and allowedFunctionNames.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/hooks/index.md" target="_blank" rel="noopener noreferrer">In-session hook management commands</a></dt>
    <dd class="provider-extensions__description">Hooks can be managed interactively without editing JSON: /hooks panel displays all configured hooks; /hooks enable-all and /hooks disable-all toggle the entire hook system; /hooks enable &lt;name&gt; and /hooks disable &lt;name&gt; toggle individual hooks by name. Changes take effect immediately within the session.</dd>
  </div>
</dl>

## Rule Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `GEMINI.md`
- **Global install path:** `~/.gemini`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Primary file:** `GEMINI.md`

### Mappings to Canonical

How Gemini CLI's native rules features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`auto_memory`](/reference/canonical-keys/auto-memory/) | memory_command: Gemini CLI /memory command saves notes that persist across sessions as rule-like content |
| [`file_imports`](/reference/canonical-keys/file-imports/) | file_imports extension: Gemini CLI supports including external file content into rule files via import directives |
| [`hierarchical_loading`](/reference/canonical-keys/hierarchical-loading/) | hierarchical_context_loading: Gemini CLI loads GEMINI.md files from multiple directory levels with defined precedence |

### Gemini CLI-specific Rules

These are Gemini CLI-specific rules behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/gemini-md.md" target="_blank" rel="noopener noreferrer">Hierarchical GEMINI.md loading</a></dt>
    <dd class="provider-extensions__description">Gemini CLI loads GEMINI.md files from three tiers in order: (1) global (~/.gemini/GEMINI.md), (2) workspace/environment context files found by searching configured workspace directories and their parent directories, and (3) just-in-time (JIT) context files discovered automatically when a tool accesses a file or directory — scanning that directory and its ancestors up to a trusted root. All found files are concatenated and sent to the model with every prompt. The CLI footer displays the number of loaded context files.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/gemini-md.md" target="_blank" rel="noopener noreferrer">Configurable context file name</a></dt>
    <dd class="provider-extensions__description">The default context filename is GEMINI.md, but users can configure alternative names or a list of names via the context.fileName property in settings.json. For example: &#123;"context": &#123;"fileName": ["AGENTS.md", "CONTEXT.md", "GEMINI.md"]&#125;&#125;. The CLI checks each name in order.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/gemini-md.md" target="_blank" rel="noopener noreferrer">File import directives</a></dt>
    <dd class="provider-extensions__description">GEMINI.md files can import content from other files using the @file.md syntax (e.g., @./components/instructions.md or @../shared/style-guide.md). Both relative and absolute paths are supported. Imported content is inlined before being sent to the model, allowing large rule sets to be modularized across multiple files.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/gemini-md.md" target="_blank" rel="noopener noreferrer">/memory management command</a></dt>
    <dd class="provider-extensions__description">The /memory command provides runtime management of loaded context files: /memory show displays the full concatenated active context; /memory reload forces a re-scan of all GEMINI.md files without restarting; /memory add &lt;text&gt; appends text directly to ~/.gemini/GEMINI.md for persistent in-session memory capture.</dd>
  </div>
</dl>

## MCP Config Conventions

### Native Format

- **File format:** JSON
- **Discovery paths:** `.gemini/settings.json`
- **Syllago install method:** JSON merge
- **Symlink support:** No
- **Config file:** `.gemini/settings.json`
- **Transports:** `stdio`, `sse`

### Mappings to Canonical

How Gemini CLI's native mcp configs features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`env_var_expansion`](/reference/canonical-keys/env-var-expansion/) | env_variable_expansion: Gemini CLI MCP configuration supports environment variable expansion for secrets and paths |
| [`resource_referencing`](/reference/canonical-keys/resource-referencing/) | resource_referencing: Gemini CLI supports accessing MCP resources in addition to tools |
| [`tool_filtering`](/reference/canonical-keys/tool-filtering/) | tool_filtering: Gemini CLI supports per-server tool filtering to control which MCP tools are exposed to the agent |
| [`transport_types`](/reference/canonical-keys/transport-types/) | transport_types: Gemini CLI documents supported MCP transport protocols including stdio and HTTP |

### Gemini CLI-specific MCP Configs

These are Gemini CLI-specific mcp configs behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/tools/mcp-server.md" target="_blank" rel="noopener noreferrer">Three transport mechanisms</a></dt>
    <dd class="provider-extensions__description">Gemini CLI supports three MCP transport types: stdio (spawns a subprocess, communicates via stdin/stdout, configured via command + optional args/cwd), SSE (Server-Sent Events, configured via url), and Streamable HTTP (configured via httpUrl). The transport type can be set explicitly via the type field (stdio/sse/http) or inferred from which connection property is present.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/schemas/settings.schema.json" target="_blank" rel="noopener noreferrer">Per-server tool allowlist and blocklist</a></dt>
    <dd class="provider-extensions__description">Each MCP server config supports includeTools (string[]) to allowlist specific tools from that server, and excludeTools (string[]) to blocklist tools. excludeTools takes precedence over includeTools — if a tool appears in both, it is excluded. When includeTools is omitted, all tools from the server are available.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/tools/mcp-server.md" target="_blank" rel="noopener noreferrer">Per-server trust flag</a></dt>
    <dd class="provider-extensions__description">The trust boolean on an MCP server configuration bypasses all tool call confirmations for that server when set to true (default: false). Trusted servers execute tools without prompting the user for approval.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/tools/mcp-server.md" target="_blank" rel="noopener noreferrer">Global MCP allowed/excluded lists</a></dt>
    <dd class="provider-extensions__description">The top-level mcp object in settings.json provides global rules across all servers: mcp.allowed (string[]) allowlists server names so only listed servers connect; mcp.excluded (string[]) blocklists server names to prevent connection. These are global filters applied before per-server configuration.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/tools/mcp-server.md" target="_blank" rel="noopener noreferrer">Environment variable expansion in env blocks</a></dt>
    <dd class="provider-extensions__description">Gemini CLI automatically expands environment variables in the env block of MCP server configs. Supports POSIX/Bash syntax ($VAR or $&#123;VAR&#125;) on all platforms and Windows syntax (%VAR%) on Windows. This allows referencing secrets from the shell environment without hardcoding them in settings.json.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/tools/mcp-server.md" target="_blank" rel="noopener noreferrer">CLI commands for MCP server management</a></dt>
    <dd class="provider-extensions__description">MCP servers can be managed via gemini mcp subcommands: mcp add (add a server), mcp remove (delete from config), mcp list (view status of all servers), mcp enable/disable (toggle without removing, with optional --session flag for session-only changes). Enable/disable state is persisted to ~/.gemini/mcp-server-enablement.json. In-session equivalents: /mcp list, /mcp enable &lt;name&gt;, /mcp disable &lt;name&gt;, /mcp reload.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/tools/mcp-server.md" target="_blank" rel="noopener noreferrer">MCP resource referencing via @ syntax</a></dt>
    <dd class="provider-extensions__description">Gemini CLI discovers MCP server resources (via resources/list) at connection time and exposes them alongside filesystem paths in the @ completion menu. Resources are referenced with @server://resource/path syntax in conversation. The CLI calls resources/read and injects the content into the conversation when referenced.</dd>
  </div>
</dl>

## Command Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.gemini/commands`
- **Global install path:** `~/.gemini/commands`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Native frontmatter fields:** `name`, `description`, `prompt`

### Mappings to Canonical

How Gemini CLI's native commands features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`argument_substitution`](/reference/canonical-keys/argument-substitution/) | &#123;&#123;args&#125;&#125; placeholder injects all user-provided arguments; processed before shell injection (!&#123;...&#125;) and other substitutions; default argument handling collapses newlines |
| [`builtin_commands`](/reference/canonical-keys/builtin-commands/) | built-in slash commands include /commands reload (reload command files without restart) and /help; user-defined commands supplement but do not replace built-ins |

### Gemini CLI-specific Commands

These are Gemini CLI-specific commands behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md" target="_blank" rel="noopener noreferrer">TOML command file format</a></dt>
    <dd class="provider-extensions__description">Commands are defined as TOML files with a .toml extension. The required field is prompt (string, single or multi-line). The optional field is description (string, one-line); if omitted, a generic description is generated from the filename. No other fields are documented in the v1 format.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md" target="_blank" rel="noopener noreferrer">Namespace via subdirectory path</a></dt>
    <dd class="provider-extensions__description">Commands are namespaced by their file path relative to the commands/ directory. Path separators (/ or \) are converted to colons in the command name. For example, commands/git/commit.toml becomes /git:commit. This allows hierarchical organization of commands without any explicit namespace syntax in the TOML file.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md" target="_blank" rel="noopener noreferrer">&#123;&#123;args&#125;&#125; argument injection placeholder</a></dt>
    <dd class="provider-extensions__description">The &#123;&#123;args&#125;&#125; placeholder in the prompt is replaced with text the user typed after the command name. When used in the main prompt body, args are injected raw. When used inside a shell injection block (!&#123;...&#125;), args are automatically shell-escaped before substitution to prevent command injection. If the prompt does not contain &#123;&#123;args&#125;&#125;, the user's full command input is appended after two newlines (default argument handling).</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md" target="_blank" rel="noopener noreferrer">Shell command injection with !&#123;...&#125;</a></dt>
    <dd class="provider-extensions__description">The !&#123;...&#125; syntax in a prompt executes a shell command and injects its stdout into the prompt before sending to the model. The parser handles nested braces and balanced-brace content. Before execution, Gemini CLI displays the resolved command and asks the user to confirm. If the command fails, the injected text includes stderr and an exit code status line. &#123;&#123;args&#125;&#125; inside !&#123;...&#125; is automatically shell-escaped.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md" target="_blank" rel="noopener noreferrer">File content injection with @&#123;...&#125;</a></dt>
    <dd class="provider-extensions__description">The @&#123;...&#125; syntax in a prompt injects file content or a directory listing directly into the prompt. Supports text files, images (PNG, JPEG), PDFs, audio, and video (as multimodal input). Directory paths inject a listing of all files recursively, respecting .gitignore and .geminiignore. File injection is processed before shell commands (!&#123;...&#125;) and argument substitution (&#123;&#123;args&#125;&#125;). Paths are workspace-aware; absolute paths within the workspace are allowed.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md" target="_blank" rel="noopener noreferrer">Project commands override user commands</a></dt>
    <dd class="provider-extensions__description">When a command name exists in both the project commands directory (&lt;project&gt;/.gemini/commands/) and the user commands directory (~/.gemini/commands/), the project command always takes precedence. This allows projects to override global commands with project-specific versions.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/google-gemini/gemini-cli/main/docs/cli/custom-commands.md" target="_blank" rel="noopener noreferrer">/commands reload for live updates</a></dt>
    <dd class="provider-extensions__description">Running /commands reload picks up newly created or modified .toml command files without restarting the CLI. This allows iterative command development within an active session.</dd>
  </div>
</dl>

## Agent Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.gemini/agents`
- **Global install path:** `~/.gemini/agents`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Native frontmatter fields:** `name`, `description`, `tools`, `model`, `max_turns`, `temperature`, `timeout_mins`, `kind`

## Detection

Syllago detects Gemini CLI by checking for the `~/.gemini` directory.

## Working with Gemini CLI

```bash
# Add content from Gemini CLI
syllago add --from gemini-cli

# Install content to Gemini CLI
syllago install my-rule --to gemini-cli
```

## See Also

- [Providers Overview](/using-syllago/providers/)
- [Format Conversion](/using-syllago/format-conversion/)

*Generated from syllago 0.7.0 on 2026-04-11.*
