---
title: Codex
description: How syllago works with Codex — supported content types, file locations, hook events, MCP configuration, and format details.
---

{/* AUTO-GENERATED — do not edit. Source: providers.json via sync-providers.ts */}

## Provider Details

| Detail | Value |
|--------|-------|
| **Slug** | `codex` |
| **Config directory** | `~/.codex` |
| **Supported content types** | [Skills](#skills-conventions), [Hooks](#hook-conventions), [Rules](#rule-conventions), [MCP Configs](#mcp-config-conventions), [Commands](#command-conventions), [Agents](#agent-conventions) |
| **Emit path** | `AGENTS.md` |

## Supported Content Types

| Content Type | Syllago Install Method | Symlink |
|--------------|------------------------|:-------:|
| [Skills](#skills-conventions) | Symlink | Yes |
| [Hooks](#hook-conventions) | Symlink | No |
| [Rules](#rule-conventions) | Symlink | Yes |
| [MCP Configs](#mcp-config-conventions) | JSON merge | No |
| [Commands](#command-conventions) | Symlink | Yes |
| [Agents](#agent-conventions) | Symlink | Yes |

## Skills Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.agents/skills`
- **Global install path:** `~/.agents/skills`
- **Syllago install method:** Symlink
- **Symlink support:** Yes

### Mappings to Canonical

How Codex's native skills features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`canonical_filename`](/reference/canonical-keys/canonical-filename/) | Fixed filename SKILL.md required within each named skill directory (SKILLS_FILENAME constant) |
| [`description`](/reference/canonical-keys/description/) | YAML frontmatter key: description (required — empty string triggers MissingField error; max 1024 chars enforced after whitespace normalization) |
| [`disable_model_invocation`](/reference/canonical-keys/disable-model-invocation/) | openai.yaml companion file key: policy.allow_implicit_invocation (optional bool; false disables implicit/auto invocation; defaults to true when absent) |
| [`display_name`](/reference/canonical-keys/display-name/) | YAML frontmatter key: name (optional — falls back to parent directory name if absent or empty; max 64 chars enforced after whitespace normalization) |
| [`global_scope`](/reference/canonical-keys/global-scope/) | Skills directory at $HOME/.agents/skills/ or the user config layer's skills/ subdir ($CODEX_HOME/skills, deprecated but kept for backward compatibility) (SkillScope::User) |
| [`project_scope`](/reference/canonical-keys/project-scope/) | Skills directory at &lt;project_config_folder&gt;/skills/ (SkillScope::Repo) or any .agents/skills/ directory between project root and cwd (monorepo traversal) |
| [`shared_scope`](/reference/canonical-keys/shared-scope/) | Admin scope at /etc/codex/skills on Unix (SkillScope::Admin) — machine-level shared skills readable by all local users; injected via System config layer |

### Codex-specific Skills

These are Codex-specific skills behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (SkillFrontmatterMetadata.short_description, resolve_interface)" target="_blank" rel="noopener noreferrer">Short Description</a></dt>
    <dd class="provider-extensions__description">A condensed version of the skill description. Declared in SKILL.md frontmatter under metadata.short-description (YAML key, hyphenated). Also available as interface.short_description in the openai.yaml companion file. Truncated to 1024 chars. Used for compact display contexts where the full description is too long.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (SKILLS_METADATA_DIR, SKILLS_METADATA_FILENAME, load_skill_metadata)" target="_blank" rel="noopener noreferrer">openai.yaml Companion Metadata File</a></dt>
    <dd class="provider-extensions__description">An optional companion file at &lt;skill_dir&gt;/agents/openai.yaml that provides UI and policy metadata separate from SKILL.md frontmatter. Contains three top-level sections: interface (display_name, short_description, icon_small, icon_large, brand_color, default_prompt), dependencies (tools list with type, value, description, transport, command, url fields), and policy (allow_implicit_invocation, products). The file is optional — missing it is not an error. Path resolution for icon assets enforces that paths must be relative and under an assets/ subdirectory of the skill dir.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (resolve_asset_path), model.rs (SkillInterface.icon_small, icon_large)" target="_blank" rel="noopener noreferrer">Skill Interface Icons</a></dt>
    <dd class="provider-extensions__description">Skills may declare small and large icons via openai.yaml interface.icon_small and interface.icon_large. Values must be relative paths under the skill's assets/ directory (absolute paths and path traversal via .. are rejected). Used for UI display.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (resolve_color_str), model.rs (SkillInterface.brand_color)" target="_blank" rel="noopener noreferrer">Skill Brand Color</a></dt>
    <dd class="provider-extensions__description">A brand color for the skill declared as interface.brand_color in openai.yaml. Must be a valid #RRGGBB hex string (exactly 7 chars). Used for UI theming of the skill card.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (resolve_interface), model.rs (SkillInterface.default_prompt)" target="_blank" rel="noopener noreferrer">Default Prompt</a></dt>
    <dd class="provider-extensions__description">A default prompt for the skill declared as interface.default_prompt in openai.yaml. Max 1024 chars. Provides a pre-filled starting prompt when a user invokes the skill from a UI that supports it.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (resolve_dependency_tool, DependencyTool), model.rs (SkillDependencies, SkillToolDependency)" target="_blank" rel="noopener noreferrer">Tool Dependencies</a></dt>
    <dd class="provider-extensions__description">Skills can declare tool dependencies via openai.yaml dependencies.tools (a list). Each dependency has: type (required, max 64 chars), value (required, max 1024 chars), description (optional, max 1024 chars), transport (optional, max 64 chars), command (optional, max 1024 chars), url (optional, max 1024 chars). Used to declare what external tools or MCP servers the skill requires.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="model.rs (SkillPolicy.products, matches_product_restriction_for_product), loader.rs (resolve_policy)" target="_blank" rel="noopener noreferrer">Product Gating</a></dt>
    <dd class="provider-extensions__description">Skills can be restricted to specific OpenAI products via openai.yaml policy.products (a list of Product values). When products is non-empty, the skill is only shown/used when the active product matches the restriction list. When products is empty, the skill is available for all products. Filtering is applied at load time via filter_skill_load_outcome_for_product().</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (skill_roots_from_layer_stack_inner, discover_skills_under_root follow_symlinks logic)" target="_blank" rel="noopener noreferrer">System Scope (Embedded Skills Cache)</a></dt>
    <dd class="provider-extensions__description">A fourth scope beyond Repo/User/Admin. System-scoped skills are embedded by Codex itself and cached under $CODEX_HOME/skills/.system (SkillScope::System). This scope does NOT follow symlinks (unlike Repo, User, and Admin scopes). Parse errors for system-scoped skills are silently suppressed (not added to SkillLoadOutcome.errors). Skills are sorted Repo &gt; User &gt; System &gt; Admin by priority.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (skill_roots_with_home_dir, namespaced_skill_name, plugin_namespace_for_skill_path)" target="_blank" rel="noopener noreferrer">Plugin Scope</a></dt>
    <dd class="provider-extensions__description">A plugin extension mechanism where additional skill roots are injected externally via plugin_skill_roots (Vec&lt;PathBuf&gt; from codex_utils_plugins). Plugin skills are assigned SkillScope::User scope and receive a namespaced name prefix (plugin_namespace_for_skill_path). This allows third-party plugins to contribute skills without modifying config files.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (repo_agents_skill_roots, dirs_between_project_root_and_cwd, MAX_SCAN_DEPTH, MAX_SKILLS_DIRS_PER_ROOT)" target="_blank" rel="noopener noreferrer">Monorepo Skill Discovery</a></dt>
    <dd class="provider-extensions__description">For Repo-scoped skills, codex walks all directories between the detected project root and the current working directory, collecting any .agents/skills/ subdirectory found along the path. Project root is determined by scanning ancestors for project_root_markers (configurable, defaults to standard VCS/build markers). MAX_SCAN_DEPTH=6 limits traversal depth within each skills root; MAX_SKILLS_DIRS_PER_ROOT=2000 caps total dirs scanned per root.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="loader.rs (load_skills_from_roots dedup and sort logic)" target="_blank" rel="noopener noreferrer">Skill Deduplication</a></dt>
    <dd class="provider-extensions__description">After loading all roots, skills with duplicate SKILL.md paths are deduplicated — only the first occurrence (highest-priority scope) is kept. Skills are then sorted by scope priority (Repo &gt; User &gt; System &gt; Admin), then by name, then by path.</dd>
  </div>
</dl>

## Hook Conventions

### Native Format

- **File format:** JSON
- **Discovery paths:** `.codex/hooks.json`
- **Global install path:** `~/.codex`
- **Syllago install method:** Symlink
- **Symlink support:** No
- **Config file:** `.codex/hooks.json`
- **Handler types:** `command`

Codex supports hooks, but syllago does not yet map its hook event names. Hook conversion to and from Codex is best-effort.

### Mappings to Canonical

How Codex's native hooks features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`async_execution`](/reference/canonical-keys/async-execution/) | hook_execution_mode: Codex supports async hook execution mode for fire-and-forget background hook runs |
| [`context_injection`](/reference/canonical-keys/context-injection/) | hook_system_message: Codex hooks can inject a system message into the agent's active session context |
| [`decision_control`](/reference/canonical-keys/decision-control/) | hook_result_abort: Codex hooks can abort (block) the triggering action; allow and modify sub-capabilities documented via hook result schema |
| [`handler_types`](/reference/canonical-keys/handler-types/) | hook_handler_types: Codex supports shell command and LLM prompt handler types; TypeScript extension type also documented |
| [`hook_scopes`](/reference/canonical-keys/hook-scopes/) | hook_scope: Codex hooks can be scoped to global/user or project configuration |
| [`input_modification`](/reference/canonical-keys/input-modification/) | hook_updated_input: Codex PreToolUse hooks return modified input arguments that replace the original tool input |
| [`matcher_patterns`](/reference/canonical-keys/matcher-patterns/) | hook_matcher: Codex hooks support pattern matching to filter which tools or events trigger the hook |
| [`permission_control`](/reference/canonical-keys/permission-control/) | hook_permission_decision: Codex hooks can return permission decisions that grant or deny tool availability |

### Codex-specific Hooks

These are Codex-specific hooks behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.rs (HookEvents struct), HookEventName.ts" target="_blank" rel="noopener noreferrer">Hook Events</a></dt>
    <dd class="provider-extensions__description">Codex supports five hook events: PreToolUse, PostToolUse, SessionStart, UserPromptSubmit, and Stop. Event names use PascalCase in the config.toml hooks file (under [hooks.PreToolUse], etc.) and camelCase in the TypeScript app-server protocol (preToolUse, postToolUse, sessionStart, userPromptSubmit, stop). In the Rust wire payload, event names serialize as PascalCase strings (HookEventNameWire enum).</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.rs (HookHandlerConfig enum), HookHandlerType.ts" target="_blank" rel="noopener noreferrer">Hook Handler Types</a></dt>
    <dd class="provider-extensions__description">Each hook entry specifies a handler type via the type field. Three types are supported: command (runs a shell command), prompt (invokes a prompt), and agent (delegates to a sub-agent). The command type is the primary type for external scripts. Command handlers support: command (required string), timeout/timeoutSec (optional duration in seconds), async (bool, default false — runs hook asynchronously without blocking the operation), and statusMessage (optional display string).</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.rs (MatcherGroup.matcher)" target="_blank" rel="noopener noreferrer">Tool Matcher (PreToolUse / PostToolUse)</a></dt>
    <dd class="provider-extensions__description">PreToolUse and PostToolUse hook entries can include an optional matcher field (a string pattern) that filters which tool calls trigger the hook. When absent, the hook fires for all tool calls. This allows attaching hooks to specific tools by name.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="HookExecutionMode.ts, config.rs (HookHandlerConfig::Command.async)" target="_blank" rel="noopener noreferrer">Hook Execution Mode (sync / async)</a></dt>
    <dd class="provider-extensions__description">Hook handlers can run synchronously (blocking the current operation until the hook completes) or asynchronously (fire-and-forget, does not block). Exposed in the TypeScript protocol as HookExecutionMode with values sync and async. In the Rust config, the async field on HookHandlerConfig::Command controls this for command handlers.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="HookScope.ts" target="_blank" rel="noopener noreferrer">Hook Scope (thread / turn)</a></dt>
    <dd class="provider-extensions__description">Hooks can be scoped to a thread (lifetime of the entire session) or a turn (single request/response cycle). Exposed in the TypeScript app-server protocol as HookScope with values thread and turn.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="types.rs (HookResult enum), pre-tool-use.command.output.schema.json" target="_blank" rel="noopener noreferrer">Hook Result Abort vs Continue</a></dt>
    <dd class="provider-extensions__description">The Rust hook engine distinguishes three outcomes: Success, FailedContinue (hook failed but subsequent hooks and the operation continue), and FailedAbort (hook failed and the operation is aborted, no further hooks run). This tri-state is more expressive than a simple pass/fail. For command hooks, the JSON output schema supports a continue boolean (default true) and a decision field (approve/block for PreToolUse, block for PostToolUse) to control operation flow.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="pre-tool-use.command.output.schema.json (suppressOutput)" target="_blank" rel="noopener noreferrer">Suppress Output Flag</a></dt>
    <dd class="provider-extensions__description">Hook command output schemas include a suppressOutput boolean (default false). When true, the hook's stdout/stderr is not surfaced to the user. Available on all five event output schemas.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="pre-tool-use.command.output.schema.json (systemMessage)" target="_blank" rel="noopener noreferrer">System Message Injection</a></dt>
    <dd class="provider-extensions__description">Hook command output schemas include a systemMessage field. When set, the hook can inject additional context into the system message for the current turn. Available on all five event output schemas.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="pre-tool-use.command.input.schema.json (turn_id description)" target="_blank" rel="noopener noreferrer">Turn ID in Hook Payload</a></dt>
    <dd class="provider-extensions__description">The hook input schemas (pre-tool-use, post-tool-use) include a turn_id field marked as a Codex extension. This exposes the active turn ID to internal turn-scoped hooks, allowing hooks to correlate their actions with specific model turns.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="pre-tool-use.command.output.schema.json (hookSpecificOutput.updatedInput)" target="_blank" rel="noopener noreferrer">Updated Tool Input (PreToolUse)</a></dt>
    <dd class="provider-extensions__description">The PreToolUse hook output schema includes an updatedInput field. When set by the hook, the tool's input is replaced with the hook-provided value before the tool executes. This allows hooks to modify or sanitize tool arguments before execution.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="pre-tool-use.command.output.schema.json (PreToolUsePermissionDecisionWire)" target="_blank" rel="noopener noreferrer">Permission Decision (PreToolUse)</a></dt>
    <dd class="provider-extensions__description">The PreToolUse hook output schema includes a permissionDecision field (allow, deny, ask) within hookSpecificOutput. This gives hooks the ability to make fine-grained permission decisions about tool execution, separate from the top-level approve/block decision.</dd>
  </div>
</dl>

## Rule Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `AGENTS.md`
- **Global install path:** `~/.codex`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Primary file:** `AGENTS.md`

### Mappings to Canonical

How Codex's native rules features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`cross_provider_recognition`](/reference/canonical-keys/cross-provider-recognition/) | agents_md_filename: Codex reads AGENTS.md files using the standard filename used by multiple providers |
| [`hierarchical_loading`](/reference/canonical-keys/hierarchical-loading/) | hierarchical_agents_md: Codex loads AGENTS.md from subdirectories with defined precedence (deeper overrides higher) |

### Codex-specific Rules

These are Codex-specific rules behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (project_doc_fallback_filenames, project_doc_max_bytes)" target="_blank" rel="noopener noreferrer">AGENTS.md Filename</a></dt>
    <dd class="provider-extensions__description">Codex loads AI instruction rules from a file named AGENTS.md (or a configurable fallback list of filenames set via config.toml's project_doc_fallback_filenames). The file is discovered by walking up from the current working directory. Multiple AGENTS.md files can exist in a hierarchy — project root, subdirectory, and home directory — and their contents are combined. The primary file is AGENTS.md; fallbacks (e.g. .agents.md, CLAUDE.md) are checked when AGENTS.md is absent.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="docs/agents_md.md (Hierarchical agents message section)" target="_blank" rel="noopener noreferrer">Hierarchical AGENTS.md (child_agents_md feature flag)</a></dt>
    <dd class="provider-extensions__description">When the child_agents_md feature flag is enabled (via [features] in config.toml), Codex appends additional guidance about AGENTS.md scope and precedence to the user instructions message, and emits that message even when no AGENTS.md is present. This implements a multi-file hierarchical rules model where rules from parent directories, project root, and home directory can be combined with scoped precedence.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (project_doc_max_bytes)" target="_blank" rel="noopener noreferrer">Project Doc Max Bytes</a></dt>
    <dd class="provider-extensions__description">A configurable cap on how many bytes are read from an AGENTS.md file, set via project_doc_max_bytes in config.toml. Prevents oversized rules files from consuming excessive context window.</dd>
  </div>
</dl>

## MCP Config Conventions

### Native Format

- **File format:** TOML
- **Syllago install method:** JSON merge
- **Symlink support:** No
- **Config file:** `.codex/config.toml`
- **Transports:** `stdio`

### Mappings to Canonical

How Codex's native mcp configs features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`auto_approve`](/reference/canonical-keys/auto-approve/) | mcp_per_tool_approval: Codex supports per-tool approval configuration to suppress confirmation prompts for trusted tools |
| [`oauth_support`](/reference/canonical-keys/oauth-support/) | mcp_oauth_support: Codex supports OAuth 2.0 authentication for remote MCP servers |
| [`tool_filtering`](/reference/canonical-keys/tool-filtering/) | mcp_enabled_disabled_tools: Codex supports per-server tool enable/disable configuration controlling which tools are exposed to the agent |

### Codex-specific MCP Configs

These are Codex-specific mcp configs behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (RawMcpServerConfig, mcp_servers)" target="_blank" rel="noopener noreferrer">MCP Servers Config (config.toml mcp_servers)</a></dt>
    <dd class="provider-extensions__description">MCP servers are configured in config.toml under the mcp_servers key, which is an object mapping server names to RawMcpServerConfig entries. Each entry supports: command (stdio binary path), url (HTTP/SSE endpoint), args (command arguments), env (environment variables), env_vars (list of env var names to pass through), env_http_headers (env var names for HTTP headers), http_headers (literal HTTP headers), bearer_token, bearer_token_env_var, cwd (working directory), enabled (bool), required (bool, fails session start if server is unreachable), startup_timeout_ms/startup_timeout_sec, tool_timeout_sec, enabled_tools (allowlist), disabled_tools (blocklist), tools (per-tool approval settings), oauth_resource, scopes (OAuth), and name (legacy display-name, kept for backward compatibility).</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (McpServerToolConfig, AppToolApproval)" target="_blank" rel="noopener noreferrer">Per-Tool Approval Mode</a></dt>
    <dd class="provider-extensions__description">Each MCP server can configure per-tool approval settings via the tools map in RawMcpServerConfig. Each tool entry (McpServerToolConfig) specifies an approval_mode that overrides the session-level approval policy for that specific tool. This allows fine-grained permission control per MCP tool without requiring global policy changes.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (mcp_oauth_callback_port, mcp_oauth_callback_url, mcp_oauth_credentials_store)" target="_blank" rel="noopener noreferrer">MCP OAuth Support</a></dt>
    <dd class="provider-extensions__description">Codex supports OAuth authentication for MCP servers. Configuration fields include mcp_oauth_callback_port (fixed port for local HTTP callback), mcp_oauth_callback_url (custom redirect URI), and mcp_oauth_credentials_store (keyring, file, or auto). Servers can specify oauth_resource and scopes fields in their config. The OAuth credential store backend is OS-specific keyring by default with file fallback.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="codex_mcp_interface.md" target="_blank" rel="noopener noreferrer">Codex MCP Server Interface (codex mcp-server)</a></dt>
    <dd class="provider-extensions__description">Codex itself exposes an experimental MCP server interface via `codex mcp-server` (or `codex-mcp-server`). This runs a JSON-RPC 2.0 server over stdio using the MCP transport protocol, exposing v2 RPCs for thread management (thread/start, thread/resume, thread/fork, thread/read, thread/list), turn control (turn/start, turn/steer, turn/interrupt), account management, config read/write, model listing, and collaboration modes. It also serves v1 compatibility methods and sends live event stream notifications via codex/event/* channels. This is distinct from consuming MCP servers — it makes Codex itself controllable as an MCP server.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (RawMcpServerConfig.enabled_tools, disabled_tools)" target="_blank" rel="noopener noreferrer">MCP Tool Allowlist and Blocklist</a></dt>
    <dd class="provider-extensions__description">Each MCP server entry in config.toml supports enabled_tools (allowlist) and disabled_tools (blocklist) arrays. When enabled_tools is set, only the listed tools are exposed from that server. disabled_tools removes specific tools from the server's offering. This allows users to surface only the relevant subset of a server's tools.</dd>
  </div>
</dl>

## Command Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.codex/commands`
- **Global install path:** `~/.codex`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Native frontmatter fields:** `description`, `argument-hint`

### Mappings to Canonical

How Codex's native commands features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|

## Agent Conventions

### Native Format

- **File format:** TOML
- **Discovery paths:** `.codex/agents`
- **Global install path:** `~/.codex`
- **Syllago install method:** Symlink
- **Symlink support:** Yes

### Mappings to Canonical

How Codex's native agents features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`agent_scopes`](/reference/canonical-keys/agent-scopes/) | built-in roles always available; user-defined roles in config.toml [agents] section; user-defined roles override built-in roles of same name |
| [`definition_format`](/reference/canonical-keys/definition-format/) | TOML role config files referenced from config.toml [agents] section; built-in roles (default/explorer/worker) always available without user config |
| [`invocation_patterns`](/reference/canonical-keys/invocation-patterns/) | spawn-agent tool at LLM discretion; LLM selects role from dynamically-built role descriptions combining built-in and user-defined roles |
| [`model_selection`](/reference/canonical-keys/model-selection/) | role config_file can override model and model_reasoning_effort; caller's current profile and model_provider preserved unless explicitly overridden |
| [`subagent_spawning`](/reference/canonical-keys/subagent-spawning/) | multi-agent V2 feature flag; max_threads controls concurrent agent count, max_depth controls nesting depth; spawn-agent tool initiates delegation |

### Codex-specific Agents

These are Codex-specific agents behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (AgentsToml, AgentRoleToml), role.rs" target="_blank" rel="noopener noreferrer">Agent Roles (config.toml [agents] section)</a></dt>
    <dd class="provider-extensions__description">Codex supports a multi-agent system where spawned sub-agents run with a named role. Roles are declared in config.toml under the agents key (AgentsToml). Each role entry (AgentRoleToml) specifies: description (human-facing role documentation used in spawn tool guidance), config_file (optional path to a role-specific config layer TOML), and nickname_candidates (candidate names for agents spawned with this role). The agents section also supports global limits: max_threads (concurrent agent threads cap), max_depth (maximum nesting depth for spawned threads, minimum 1), and job_max_runtime_seconds (default max runtime for agent job workers).</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="role.rs (built_in::configs(), DEFAULT_ROLE_NAME, explorer, worker, awaiter)" target="_blank" rel="noopener noreferrer">Built-in Agent Roles (default, explorer, worker)</a></dt>
    <dd class="provider-extensions__description">Codex ships three built-in agent roles that are always available without user configuration. default: the standard agent with no special config. explorer: a fast, read-focused agent for codebase questions, runs with model_reasoning_effort=low and uses explorer.toml config; description encourages parallel spawning for independent questions. worker: an execution agent for implementing features, fixing bugs, and splitting large refactors; description emphasizes ownership assignment and awareness of parallel workers. A fourth role, awaiter (for monitoring long-running commands), exists as a built-in config (awaiter.toml) but is currently commented out of the active built-in registry.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="role.rs (apply_role_to_config, apply_role_to_config_inner, preservation_policy)" target="_blank" rel="noopener noreferrer">Role Config Layer</a></dt>
    <dd class="provider-extensions__description">When a role specifies a config_file, that file is loaded as a high-precedence config layer on top of the active session config. This allows roles to override model, reasoning effort, developer instructions, and other settings without modifying the user's base config. The role layer is inserted at session-flag precedence. The caller's current profile and model_provider are preserved unless the role explicitly selects them. Built-in role configs are embedded as static strings in the binary; user-defined role configs are read from disk paths relative to the config.toml that defines them.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="config.schema.json (MultiAgentV2ConfigToml, FeatureToml_for_MultiAgentV2ConfigToml)" target="_blank" rel="noopener noreferrer">Multi-Agent V2 Feature Flag</a></dt>
    <dd class="provider-extensions__description">The multi-agent system is controlled by a feature flag: features.multi_agent_v2 in config.toml (FeatureToml_for_MultiAgentV2ConfigToml — either a boolean or a config object). The config object form supports: enabled (bool), hide_spawn_agent_metadata (bool), usage_hint_enabled (bool), and usage_hint_text (string). This allows operators to enable/disable multi-agent spawning and control how spawn metadata is surfaced.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="role.rs (spawn_tool_spec::build, build_from_configs, format_role)" target="_blank" rel="noopener noreferrer">Spawn Tool Role Description Generation</a></dt>
    <dd class="provider-extensions__description">At session start, Codex dynamically builds the spawn-agent tool description by combining built-in and user-defined role descriptions and their locked settings (model and reasoning_effort if set in the role config). User-defined roles take precedence over built-in roles with the same name. This description is shown to the LLM to guide when and how to spawn sub-agents.</dd>
  </div>
</dl>

## Detection

Syllago detects Codex by checking for the `~/.codex` directory.

## Working with Codex

```bash
# Add content from Codex
syllago add --from codex

# Install content to Codex
syllago install my-rule --to codex
```

## See Also

- [Providers Overview](/using-syllago/providers/)
- [Format Conversion](/using-syllago/format-conversion/)

*Generated from syllago 0.7.0 on 2026-04-11.*
