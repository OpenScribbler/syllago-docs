---
title: Codex
description: How syllago works with Codex — supported content types, file locations, hook events, MCP configuration, and format details.
---

{/* AUTO-GENERATED — do not edit. Source: providers.json via sync-providers.ts */}

import ProviderExtensions from '../../../../components/ProviderExtensions.astro';

## Provider Details

| Detail | Value |
|--------|-------|
| **Slug** | `codex` |
| **Config directory** | `~/.codex` |
| **Supported content types** | Agents, Commands, Hooks, MCP Configs, Rules, Skills |
| **Emit path** | `AGENTS.md` |

## Supported Content Types

| Content Type | Supported | Install Method |
|-------------|-----------|---------------|
| Rules | Yes | Symlink |
| Skills | Yes | Symlink |
| Agents | Yes | Symlink |
| MCP Configs | Yes | JSON merge |
| Hooks | Yes | Symlink |
| Commands | Yes | Symlink |
| Loadouts | No | — |

## File Format and Location

| Content Type | Discovery Paths | Global Location | Format |
|-------------|----------------|----------------|--------|
| Rules | `AGENTS.md` | `~/.codex` | Markdown |
| Skills | `.agents/skills` | `~/.agents/skills` | Markdown |
| Agents | `.codex/agents` | `~/.codex` | TOML |
| MCP Configs | — | — | TOML |
| Hooks | `.codex/hooks.json` | `~/.codex` | JSON |
| Commands | `.codex/commands` | `~/.codex` | Markdown |

## Hooks

Codex supports hooks, but syllago does not yet map its hook event names. Hook conversion to and from Codex is best-effort.

Hooks are configured in `.codex/hooks.json`.


## MCP Configuration

| Detail | Value |
|--------|-------|
| **Transports** | `stdio` |
| **Config file** | `.codex/config.toml` |

## Skills Extensions

Provider-specific skills behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="codex" contentType="skills" />

## Rules Extensions

Provider-specific rules behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="codex" contentType="rules" />

## Agents Extensions

Provider-specific agents behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="codex" contentType="agents" />

## Hooks Extensions

Provider-specific hooks behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="codex" contentType="hooks" />

## MCP Configs Extensions

Provider-specific mcp configs behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="codex" contentType="mcp" />


## Detection

Syllago detects Codex by checking for the `~/.codex` directory.

## Working with Codex

```bash
# Add content from Codex
syllago add --from codex

# Install content to Codex
syllago install my-rule --to codex
```

## See Also

- [Providers Overview](/using-syllago/providers/)
- [Format Conversion](/using-syllago/format-conversion/)

*Generated from syllago dev on 2026-04-13.*
