---
title: Kiro
description: How syllago works with Kiro — supported content types, file locations, hook events, MCP configuration, and format details.
---

{/* AUTO-GENERATED — do not edit. Source: providers.json via sync-providers.ts */}

## Provider Details

| Detail | Value |
|--------|-------|
| **Slug** | `kiro` |
| **Config directory** | `~/.kiro` |
| **Supported content types** | [Skills](#skills-conventions), [Hooks](#hook-conventions), [Rules](#rule-conventions), [MCP Configs](#mcp-config-conventions), [Agents](#agent-conventions) |
| **Emit path** | `.kiro/steering` |

## Supported Content Types

| Content Type | Syllago Install Method | Symlink |
|--------------|------------------------|:-------:|
| [Skills](#skills-conventions) | Project scope | Yes |
| [Hooks](#hook-conventions) | JSON merge | No |
| [Rules](#rule-conventions) | Project scope | Yes |
| [MCP Configs](#mcp-config-conventions) | JSON merge | No |
| [Agents](#agent-conventions) | Symlink | Yes |

## Skills Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.kiro/steering`
- **Syllago install method:** Project scope
- **Symlink support:** Yes
- **Native frontmatter fields:** `name`, `description`, `license`, `compatibility`, `metadata`

### Mappings to Canonical

How Kiro's native skills features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`canonical_filename`](/reference/canonical-keys/canonical-filename/) | Fixed filename POWER.md (required, all caps) |
| [`compatibility`](/reference/canonical-keys/compatibility/) | yaml frontmatter key: compatibility (optional) |
| [`description`](/reference/canonical-keys/description/) | yaml frontmatter key: description (required) |
| [`display_name`](/reference/canonical-keys/display-name/) | yaml frontmatter key: name (required, slug-style identifier) |
| [`license`](/reference/canonical-keys/license/) | yaml frontmatter key: license (optional) |
| [`metadata_map`](/reference/canonical-keys/metadata-map/) | yaml frontmatter key: metadata (optional map) |
| [`project_scope`](/reference/canonical-keys/project-scope/) | Self-contained directory installed via Kiro Powers panel (UI installation, no fixed filesystem path) |

### Kiro-specific Skills

These are Kiro-specific skills behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/powers/create/" target="_blank" rel="noopener noreferrer">displayName frontmatter field</a></dt>
    <dd class="provider-extensions__description">A human-readable label separate from the slug-style 'name' field. Kiro uses both: 'name' as the slug identifier and 'displayName' for UI display.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/powers/create/" target="_blank" rel="noopener noreferrer">keywords frontmatter field</a></dt>
    <dd class="provider-extensions__description">An array of activation trigger words used by Kiro's automatic power-loading system (e.g. ["database", "postgres", "auth"]).</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/powers/create/" target="_blank" rel="noopener noreferrer">steering/ directory</a></dt>
    <dd class="provider-extensions__description">An optional steering/ subdirectory inside the power directory containing workflow-specific guidance files (.md). For complex powers, the POWER.md maps each workflow to a specific steering file (e.g., 'Setting up a database → database-setup-workflow.md'). Kiro loads only the relevant steering file on-demand based on what the user is working on, preventing context overload. Simple powers include all guidance directly in POWER.md instead.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/powers/create/" target="_blank" rel="noopener noreferrer">mcp.json bundled in power directory</a></dt>
    <dd class="provider-extensions__description">An optional mcp.json file inside the power directory that declares MCP server configurations for tool integrations (mcpServers map with command, args, and env). Server names in POWER.md must match the mcpServers keys in mcp.json. During installation, Kiro automatically namespaces server names to avoid conflicts (e.g., supabase-local becomes power-supabase-supabase-local). Environment variables are used for API keys and secrets.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/powers/create/" target="_blank" rel="noopener noreferrer">Onboarding section in POWER.md</a></dt>
    <dd class="provider-extensions__description">A dedicated # Onboarding section in POWER.md whose instructions run when a power is first activated. Used to validate dependencies (e.g., check Docker is installed and running), explain setup steps, or create workspace hooks. Kiro executes these instructions automatically — for example, checking if Docker is running, validating CLI installation, and creating hook files in .kiro/hooks/.</dd>
  </div>
</dl>

## Hook Conventions

### Native Format

- **File format:** JSON
- **Discovery paths:** `.kiro/agents`
- **Syllago install method:** JSON merge
- **Symlink support:** No
- **Config file:** `.kiro/agents/`
- **Handler types:** `command`
- **Hook events:** 10

| Canonical Event | Native Name | Category |
|-----------------|-------------|----------|
| `after_task` | `Post Task Execution` | Lifecycle |
| `after_tool_execute` | `postToolUse` | Tool |
| `agent_stop` | `stop` | Lifecycle |
| `before_prompt` | `userPromptSubmit` | Lifecycle |
| `before_task` | `Pre Task Execution` | Lifecycle |
| `before_tool_execute` | `preToolUse` | Tool |
| `file_changed` | `File Save` | Workspace |
| `file_created` | `File Create` | Workspace |
| `file_deleted` | `File Delete` | Workspace |
| `session_start` | `agentSpawn` | Lifecycle |

### Mappings to Canonical

How Kiro's native hooks features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|

### Kiro-specific Hooks

These are Kiro-specific hooks behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/hooks/" target="_blank" rel="noopener noreferrer">Predefined agent prompt as hook action</a></dt>
    <dd class="provider-extensions__description">Hook actions can be a predefined agent prompt (not a shell command). When the trigger fires, Kiro runs the specified prompt through its agent instead of executing a shell command. This is in addition to standard shell command actions.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/hooks/" target="_blank" rel="noopener noreferrer">Spec task lifecycle triggers (before/after spec task execution)</a></dt>
    <dd class="provider-extensions__description">Hooks can fire before and after spec task execution — Kiro-specific triggers tied to its spec/planning workflow (spec tasks are a distinct Kiro concept). Not related to standard file or tool events.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/hooks/" target="_blank" rel="noopener noreferrer">Agent turn completion trigger</a></dt>
    <dd class="provider-extensions__description">A hook trigger that fires after each agent turn completes. Allows automated follow-up actions after every agent response in a session.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/hooks/" target="_blank" rel="noopener noreferrer">Hooks configured via IDE UI only</a></dt>
    <dd class="provider-extensions__description">Kiro hooks ('Agent hooks') are created and managed exclusively through the IDE panel or by asking Kiro in natural language ('Ask Kiro to create a hook'). No documented config file format — hooks are IDE-panel state, not portable YAML or JSON files.</dd>
  </div>
</dl>

## Rule Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.kiro/steering`
- **Syllago install method:** Project scope
- **Symlink support:** Yes
- **Primary file:** `.kiro/steering`
- **Native frontmatter fields:** `inclusion`, `fileMatchPattern`, `name`, `description`

### Mappings to Canonical

How Kiro's native rules features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`activation_mode`](/reference/canonical-keys/activation-mode/) | kiro_steering_inclusion_mode: Kiro steering files support always, conditional, and manual inclusion modes |
| [`cross_provider_recognition`](/reference/canonical-keys/cross-provider-recognition/) | kiro_agents_md_recognition: Kiro reads AGENTS.md files from other providers in addition to its own steering files |
| [`file_imports`](/reference/canonical-keys/file-imports/) | kiro_steering_file_references: Kiro steering files can reference other files using @file syntax |

### Kiro-specific Rules

These are Kiro-specific rules behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/steering/" target="_blank" rel="noopener noreferrer">Inclusion mode frontmatter field</a></dt>
    <dd class="provider-extensions__description">Controls when a steering file is loaded into context. Four modes: 'always' (default — included in every agent interaction), 'conditional' (loaded when the current file matches a 'globs' frontmatter pattern), 'manual' (only loaded when the user explicitly requests it), 'auto' (Kiro decides based on relevance, similar to model-driven auto-invocation). The 'conditional' and 'auto' modes enable selective context loading to avoid context overload.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/steering/" target="_blank" rel="noopener noreferrer">File references (@path/to/file) in steering files</a></dt>
    <dd class="provider-extensions__description">Steering files can include references to other files using '@path/to/file' syntax, causing Kiro to inline the referenced file's content into the steering context. Enables modular steering composition without duplicating content.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/steering/" target="_blank" rel="noopener noreferrer">Foundational steering files (auto-created)</a></dt>
    <dd class="provider-extensions__description">Kiro automatically creates three foundational steering files on workspace initialization: product_requirements.md, tech_stack.md, and architecture.md. These serve as a structured project context baseline and are created by Kiro, not the user.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/steering/" target="_blank" rel="noopener noreferrer">AGENTS.md root file recognition</a></dt>
    <dd class="provider-extensions__description">Kiro recognizes AGENTS.md in the repo root as a steering source, consistent with the cross-provider AGENTS.md convention. Treated as a workspace-scoped steering file alongside .kiro/steering/ content.</dd>
  </div>
</dl>

## MCP Config Conventions

### Native Format

- **File format:** JSON
- **Discovery paths:** `.kiro/settings/mcp.json`
- **Syllago install method:** JSON merge
- **Symlink support:** No
- **Config file:** `.kiro/settings/mcp.json`
- **Transports:** `stdio`, `sse`

### Mappings to Canonical

How Kiro's native mcp configs features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`auto_approve`](/reference/canonical-keys/auto-approve/) | kiro_mcp_auto_approve: Kiro supports per-server or per-tool auto-approval configuration to suppress confirmation prompts |
| [`tool_filtering`](/reference/canonical-keys/tool-filtering/) | kiro_mcp_disabled_tools: Kiro supports per-server tool disable configuration to control which MCP tools are available to the agent |

### Kiro-specific MCP Configs

These are Kiro-specific mcp configs behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/mcp/configuration/" target="_blank" rel="noopener noreferrer">autoApprove per-server tool list (or wildcard)</a></dt>
    <dd class="provider-extensions__description">Each mcpServers entry can include an 'autoApprove' field: either an array of specific tool names to auto-approve without user confirmation, or the string '*' to auto-approve all tools on that server. This is per-server granularity, more fine-grained than a global approve-all setting.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/mcp/configuration/" target="_blank" rel="noopener noreferrer">disabledTools per-server list</a></dt>
    <dd class="provider-extensions__description">Each mcpServers entry can include a 'disabledTools' array to explicitly suppress specific tools from a server without disabling the entire server. Complements 'autoApprove' for fine-grained tool permission management.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/mcp/configuration/" target="_blank" rel="noopener noreferrer">disabled flag per server entry</a></dt>
    <dd class="provider-extensions__description">Each mcpServers entry can include a 'disabled: true' flag to deactivate a server without removing its configuration. Allows toggling servers on/off without editing config structure.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/mcp/configuration/" target="_blank" rel="noopener noreferrer">Remote server support (url + headers)</a></dt>
    <dd class="provider-extensions__description">In addition to stdio servers (command + args), Kiro supports remote MCP servers configured with a 'url' field (HTTPS, or HTTP for localhost) and an optional 'headers' map for authentication tokens. Env var expansion uses $&#123;VARIABLE&#125; syntax in config values.</dd>
  </div>
</dl>

## Agent Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.kiro/agents`
- **Global install path:** `~/.kiro/agents`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Native frontmatter fields:** `name`, `description`, `model`, `tools`, `allowedTools`, `toolAliases`, `toolsSettings`, `mcpServers`, `resources`, `includeMcpJson`, `includePowers`, `keyboardShortcut`, `welcomeMessage`, `hooks`

### Mappings to Canonical

How Kiro's native agents features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`agent_scopes`](/reference/canonical-keys/agent-scopes/) | project (.kiro/agents/) + global (~/.kiro/agents/); global overrides local on name collision — reverse of most Kiro content types |
| [`definition_format`](/reference/canonical-keys/definition-format/) | JSON config files in .kiro/agents/ (project) or ~/.kiro/agents/ (global); prompt field accepts inline string or file:// URI to external .md file |
| [`invocation_patterns`](/reference/canonical-keys/invocation-patterns/) | keyboard shortcut binding for IDE invocation; welcomeMessage shown on agent start; manual selection via agent picker |
| [`model_selection`](/reference/canonical-keys/model-selection/) | model field in agent JSON config overrides workspace default for that agent |
| [`per_agent_mcp`](/reference/canonical-keys/per-agent-mcp/) | inline mcpServers map per agent config; includeMcpJson boolean controls whether workspace .kiro/mcp.json is merged into agent's server set |
| [`tool_restrictions`](/reference/canonical-keys/tool-restrictions/) | tools array with exact names, wildcards (computer_*), or MCP-namespaced refs (mcp:serverName:toolName); toolsSettings for per-tool config objects |

### Kiro-specific Agents

These are Kiro-specific agents behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">File URI for externalizing agent prompt</a></dt>
    <dd class="provider-extensions__description">The 'prompt' field accepts either an inline string or a 'file://path/to/system-prompt.md' URI to load the prompt from an external Markdown file. Enables long or complex system prompts to be maintained as separate files without embedding them in the JSON config.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">toolAliases for collision remapping</a></dt>
    <dd class="provider-extensions__description">A 'toolAliases' map that renames tools to resolve naming collisions. When two MCP servers expose tools with the same name, toolAliases provides a per-agent remapping so both tools remain accessible under distinct names.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">Resources attachment (files, skills, knowledge-base)</a></dt>
    <dd class="provider-extensions__description">Agents can declare a 'resources' array attaching contextual inputs: individual files (type: file, path), skills/powers (type: skill, name), or knowledge-base entries (type: knowledge-base, name). Resources are loaded into the agent's context alongside its system prompt.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">Inline mcpServers map per agent</a></dt>
    <dd class="provider-extensions__description">Agents can declare their own 'mcpServers' map directly in the agent JSON config (same schema as .kiro/mcp.json), scoping specific MCP server configurations to that agent without affecting the workspace-wide MCP config.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">includeMcpJson flag</a></dt>
    <dd class="provider-extensions__description">A boolean flag ('includeMcpJson') that controls whether the workspace .kiro/mcp.json config is merged into this agent's available MCP servers. Defaults to true; set to false to give an agent an isolated server set using only its inline mcpServers.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">toolsSettings per-tool configuration map</a></dt>
    <dd class="provider-extensions__description">A 'toolsSettings' map that provides per-tool configuration objects, allowing fine-grained behavioral settings for individual tools available to the agent. Tool-specific options vary by tool type.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">keyboardShortcut binding</a></dt>
    <dd class="provider-extensions__description">Agents can declare a 'keyboardShortcut' string to bind a keyboard shortcut for quick agent invocation from the IDE. IDE-level keybinding, not relevant to portable agent config.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://kiro.dev/docs/cli/custom-agents/configuration-reference/" target="_blank" rel="noopener noreferrer">welcomeMessage shown on agent start</a></dt>
    <dd class="provider-extensions__description">An optional 'welcomeMessage' string displayed to the user when the agent session starts. Provides onboarding context or usage instructions at session open.</dd>
  </div>
</dl>

## Detection

Syllago detects Kiro by checking for the `~/.kiro` directory.

## Working with Kiro

```bash
# Add content from Kiro
syllago add --from kiro

# Install content to Kiro
syllago install my-rule --to kiro
```

## See Also

- [Providers Overview](/using-syllago/providers/)
- [Format Conversion](/using-syllago/format-conversion/)

*Generated from syllago 0.7.0 on 2026-04-11.*
