---
title: Kiro
description: How syllago works with Kiro — supported content types, file locations, hook events, MCP configuration, and format details.
---

{/* AUTO-GENERATED — do not edit. Source: providers.json via sync-providers.ts */}

import ProviderExtensions from '../../../../components/ProviderExtensions.astro';

## Provider Details

| Detail | Value |
|--------|-------|
| **Slug** | `kiro` |
| **Config directory** | `~/.kiro` |
| **Supported content types** | Agents, Hooks, MCP Configs, Rules, Skills |
| **Emit path** | `.kiro/steering` |

## Supported Content Types

| Content Type | Supported | Install Method |
|-------------|-----------|---------------|
| Rules | Yes | Project scope |
| Skills | Yes | Project scope |
| Agents | Yes | Symlink |
| MCP Configs | Yes | JSON merge |
| Hooks | Yes | JSON merge |
| Commands | No | — |
| Loadouts | No | — |

## File Format and Location

| Content Type | Discovery Paths | Global Location | Format |
|-------------|----------------|----------------|--------|
| Rules | `.kiro/steering` | — | Markdown |
| Skills | `.kiro/steering` | — | Markdown |
| Agents | `.kiro/agents` | `~/.kiro/agents` | Markdown |
| MCP Configs | `.kiro/settings/mcp.json` | — | JSON |
| Hooks | `.kiro/agents` | — | JSON |

## Hook Events

Kiro supports **10 hook events** with handler types: `command`.

Hooks are configured in `.kiro/agents/`.

| Event | Native Name | Category |
|-------|------------|----------|
| `after_task` | `Post Task Execution` | Lifecycle |
| `after_tool_execute` | `postToolUse` | Tool |
| `agent_stop` | `stop` | Lifecycle |
| `before_prompt` | `userPromptSubmit` | Lifecycle |
| `before_task` | `Pre Task Execution` | Lifecycle |
| `before_tool_execute` | `preToolUse` | Tool |
| `file_changed` | `File Save` | Workspace |
| `file_created` | `File Create` | Workspace |
| `file_deleted` | `File Delete` | Workspace |
| `session_start` | `agentSpawn` | Lifecycle |

## MCP Configuration

| Detail | Value |
|--------|-------|
| **Transports** | `stdio`, `sse` |
| **Config file** | `.kiro/settings/mcp.json` |


## Rules Format

| Detail | Value |
|--------|-------|
| **File format** | Markdown |
| **Frontmatter fields** | `inclusion`, `fileMatchPattern`, `name`, `description` |
| **Primary file** | `.kiro/steering` |

## Skills Extensions

Provider-specific skills behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="kiro" contentType="skills" />

## Rules Extensions

Provider-specific rules behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="kiro" contentType="rules" />

## Agents Extensions

Provider-specific agents behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="kiro" contentType="agents" />

## Hooks Extensions

Provider-specific hooks behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="kiro" contentType="hooks" />

## MCP Configs Extensions

Provider-specific mcp configs behaviors and configuration options beyond the canonical keys.

<ProviderExtensions provider="kiro" contentType="mcp" />


## Detection

Syllago detects Kiro by checking for the `~/.kiro` directory.

## Working with Kiro

```bash
# Add content from Kiro
syllago add --from kiro

# Install content to Kiro
syllago install my-rule --to kiro
```

## See Also

- [Providers Overview](/using-syllago/providers/)
- [Format Conversion](/using-syllago/format-conversion/)

*Generated from syllago dev on 2026-04-13.*
