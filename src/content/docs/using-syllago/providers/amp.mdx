---
title: Amp
description: How syllago works with Amp — supported content types, file locations, hook events, MCP configuration, and format details.
---

{/* AUTO-GENERATED — do not edit. Source: providers.json via sync-providers.ts */}

## Provider Details

| Detail | Value |
|--------|-------|
| **Slug** | `amp` |
| **Config directory** | `~/.config/amp` |
| **Supported content types** | [Skills](#skills-conventions), [Rules](#rule-conventions), [MCP Configs](#mcp-config-conventions) |
| **Emit path** | `AGENTS.md` |

## Supported Content Types

| Content Type | Syllago Install Method | Symlink |
|--------------|------------------------|:-------:|
| [Skills](#skills-conventions) | Symlink | Yes |
| [Rules](#rule-conventions) | Symlink | Yes |
| [MCP Configs](#mcp-config-conventions) | JSON merge | No |

## Skills Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `.agents/skills`, `.claude/skills`
- **Global install path:** `~/.config/agents/skills`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Native frontmatter fields:** `name`, `description`

### Mappings to Canonical

How Amp's native skills features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`canonical_filename`](/reference/canonical-keys/canonical-filename/) | SKILL.md (required, fixed name) |
| [`description`](/reference/canonical-keys/description/) | yaml frontmatter key: description (required) |
| [`display_name`](/reference/canonical-keys/display-name/) | yaml frontmatter key: name (required) |
| [`global_scope`](/reference/canonical-keys/global-scope/) | skill directory placed under ~/.config/agents/skills/&lt;name&gt;/, ~/.config/amp/skills/&lt;name&gt;/, or ~/.claude/skills/&lt;name&gt;/ |
| [`project_scope`](/reference/canonical-keys/project-scope/) | skill directory placed under .agents/skills/&lt;name&gt;/ or .claude/skills/&lt;name&gt;/ within the project root |

### Amp-specific Skills

These are Amp-specific skills behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/agent-skills.md" target="_blank" rel="noopener noreferrer">MCP server bundling</a></dt>
    <dd class="provider-extensions__description">Skills can include an mcp.json file to bundle an MCP server. The server starts when Amp launches but its tools remain hidden until the skill is loaded, keeping the tool list clean. Supports local command-based servers (command, args, env fields) and remote HTTP servers (url, headers fields). The optional includeTools field filters which tools are exposed.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/agent-skills.md" target="_blank" rel="noopener noreferrer">Executable tool bundling via toolbox/</a></dt>
    <dd class="provider-extensions__description">Skills can bundle executable tools by placing them in a toolbox/ subdirectory within the skill directory. These executables are auto-discovered and registered as tools when the skill is loaded. They follow the same stdin/stdout JSON contract as standalone toolbox tools. Tools from skills are hidden until the skill is loaded.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/agent-skills.md" target="_blank" rel="noopener noreferrer">Bundled passive resources</a></dt>
    <dd class="provider-extensions__description">Skills can include arbitrary files (scripts, templates, config files, etc.) in the same directory as SKILL.md. The agent can access these files at paths relative to the skill file. This is distinct from toolbox/ executables — bundled resources are passive files read by the agent, not registered tools.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/agent-skills.md" target="_blank" rel="noopener noreferrer">CLI and command palette skill management</a></dt>
    <dd class="provider-extensions__description">Skills are managed via the 'amp skill' CLI command and the command palette (Ctrl+O in CLI, Cmd+Shift+P in VS Code). Supported operations: 'skill: add' (install from GitHub URL, git URL, or local path), 'skill: list' (view installed skills), 'skill: remove' (remove a skill). The add command accepts shorthand GitHub paths (e.g., ampcode/amp-contrib/tmux).</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/agent-skills.md" target="_blank" rel="noopener noreferrer">Automatic model-driven invocation</a></dt>
    <dd class="provider-extensions__description">The model automatically invokes a skill when the user's request matches the skill's name and description. Both frontmatter fields are always visible to the model at startup and determine when it selects the skill — the full SKILL.md body is only loaded once invoked. This is the primary invocation path; users describe what they want and the model selects the matching skill without explicit syntax.</dd>
  </div>
</dl>

## Rule Conventions

### Native Format

- **File format:** Markdown
- **Discovery paths:** `AGENTS.md`, `AGENT.md`, `CLAUDE.md`
- **Global install path:** `~/.config/amp`
- **Syllago install method:** Symlink
- **Symlink support:** Yes
- **Primary file:** `AGENTS.md`
- **Native frontmatter fields:** `globs`

### Mappings to Canonical

How Amp's native rules features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|

### Amp-specific Rules

These are Amp-specific rules behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/sourcegraph/amp-examples-and-guides/main/guides/agent-file/AGENT.md" target="_blank" rel="noopener noreferrer">Canonical filename: AGENT.md</a></dt>
    <dd class="provider-extensions__description">Amp rules files are named AGENT.md. The filename is fixed — no frontmatter, no directory wrapper. The file contains plain Markdown with behavioral instructions, coding conventions, workflow guidelines, and any other context the agent should follow when working in the project.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/sourcegraph/amp-examples-and-guides/main/guides/agent-file/AGENT.md" target="_blank" rel="noopener noreferrer">Project-scope placement at repository root</a></dt>
    <dd class="provider-extensions__description">AGENT.md is placed at the root of the project repository. It is project-scoped: the instructions apply when Amp operates within that project. The file can be committed to version control so the whole team benefits from the same guidelines.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/sourcegraph/amp-examples-and-guides/main/guides/agent-file/AGENT.md" target="_blank" rel="noopener noreferrer">Plain Markdown, no frontmatter</a></dt>
    <dd class="provider-extensions__description">AGENT.md contains plain Markdown only — no YAML frontmatter, no structured metadata fields. Sections are organized with Markdown headings. Typical content observed includes: build commands and scripts, architecture and directory layout, code style and conventions, testing methodology (e.g., TDD requirements), PR workflow rules, tool and dependency notes, and any team-specific behavioral instructions for the AI.</dd>
  </div>
</dl>

## MCP Config Conventions

### Native Format

- **File format:** JSON
- **Discovery paths:** `.amp/settings.json`
- **Syllago install method:** JSON merge
- **Symlink support:** No
- **Config file:** `.amp/settings.json`
- **Transports:** `stdio`, `sse`

### Mappings to Canonical

How Amp's native mcp configs features map to syllago's canonical keys. See the [capabilities matrix](/reference/capabilities-matrix/) for the full vocabulary.

| Canonical Key | Mechanism |
|---------------|-----------|
| [`enterprise_management`](/reference/canonical-keys/enterprise-management/) | enterprise_registry_allowlist: Amp supports organization-level MCP configuration with an enterprise registry and allowlist |
| [`oauth_support`](/reference/canonical-keys/oauth-support/) | oauth_support: Amp supports OAuth 2.0 authentication for remote MCP servers |
| [`tool_filtering`](/reference/canonical-keys/tool-filtering/) | per_tool_enable_disable: Amp supports per-tool enable/disable configuration to control which MCP tools are exposed to the agent |

### Amp-specific MCP Configs

These are Amp-specific mcp configs behaviors and configuration options that haven't been mapped to canonical keys yet. When a canonical key covers one of these, the corresponding item graduates there.

<dl class="provider-extensions not-content">
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/mcp.md" target="_blank" rel="noopener noreferrer">MCP server configuration: amp.mcpServers</a></dt>
    <dd class="provider-extensions__description">MCP servers are configured under the 'amp.mcpServers' key in the Amp settings file. Each server entry is keyed by a name and supports two transport modes: local servers use 'command', 'args', and 'env' fields; remote servers use 'url' and 'headers' fields. Environment variable values support '$' syntax for referencing shell env vars. An optional 'disabled' boolean field can be set to temporarily disable a server without removing its config.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/sourcegraph/amp-examples-and-guides/main/guides/mcp/amp-mcp-setup-guide.md" target="_blank" rel="noopener noreferrer">Settings file locations by platform</a></dt>
    <dd class="provider-extensions__description">The Amp settings file containing 'amp.mcpServers' lives at: Windows: '%APPDATA%\amp\settings.json'; macOS and Linux: '~/.config/amp/settings.json'. The project-scoped workspace settings file is '.amp/settings.json' at the project root.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/mcp.md" target="_blank" rel="noopener noreferrer">CLI: amp mcp add</a></dt>
    <dd class="provider-extensions__description">MCP servers can be added via the CLI: 'amp mcp add &lt;name&gt; -- &lt;command&gt; [args...]' for local command-based servers, or 'amp mcp add &lt;name&gt; &lt;url&gt;' for remote HTTP servers. The double-dash separator is required before the command and its arguments for local servers. This writes the server entry into the user-level settings file.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/mcp.md" target="_blank" rel="noopener noreferrer">MCP server loading precedence</a></dt>
    <dd class="provider-extensions__description">When the same MCP server name appears in multiple configuration sources, Amp uses this precedence (highest to lowest): (1) CLI flags ('--mcp-config'), (2) user/workspace config ('amp.mcpServers'), (3) skills (only loaded if not already configured above). This means user or CLI config can override skill-provided MCP servers.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/mcp.md" target="_blank" rel="noopener noreferrer">Workspace MCP server trust approval</a></dt>
    <dd class="provider-extensions__description">MCP servers defined in workspace settings ('.amp/settings.json') require explicit approval before they can run, preventing untrusted code from executing automatically when a project is opened. A server awaiting approval appears as 'awaiting approval' in 'amp mcp doctor' output. To approve: 'amp mcp approve &lt;server-name&gt;'. MCP servers in global user settings ('~/.config/amp/settings.json') or passed via '--mcp-config' do not require approval.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/mcp.md" target="_blank" rel="noopener noreferrer">OAuth authentication for remote MCP servers</a></dt>
    <dd class="provider-extensions__description">Amp supports OAuth for remote MCP servers. For servers that implement automatic OAuth client registration (e.g., Linear), Amp starts the OAuth browser flow automatically at startup. For servers requiring manual OAuth setup: (1) create an OAuth client in the server's admin interface with redirect URI 'http://localhost:8976/oauth/callback'; (2) add the server via 'amp mcp add'; (3) register credentials via 'amp mcp oauth login &lt;server&gt; --server-url &lt;url&gt; --client-id &lt;id&gt; --client-secret &lt;secret&gt; --scopes &lt;scopes&gt;'. OAuth tokens are stored in '~/.amp/oauth/' and auto-refreshed. To clear stored tokens: 'amp mcp oauth logout &lt;server&gt;'.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/sourcegraph/amp-examples-and-guides/main/guides/mcp/amp-mcp-setup-guide.md" target="_blank" rel="noopener noreferrer">Per-tool enable/disable</a></dt>
    <dd class="provider-extensions__description">Within a configured MCP server, individual tools can be disabled without removing the server. In VS Code, tools are listed after adding a server and individual tools can be unchecked. The mcp-1 source also describes using the 'amp mcp doctor' command to inspect server and tool status. The setup guide explicitly shows unchecking specific tools (e.g., disabling 'create_issue' on the Linear server) as a context-management best practice.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://raw.githubusercontent.com/sourcegraph/amp-examples-and-guides/main/guides/mcp/amp-mcp-setup-guide.md" target="_blank" rel="noopener noreferrer">VS Code extension UI for MCP setup</a></dt>
    <dd class="provider-extensions__description">In VS Code with the Amp extension, MCP servers can be configured via the settings UI: Settings → Extensions → Amp → MCP Servers → 'Add MCP Server'. This UI accepts server name, command or URL, and arguments, and displays the list of available tools after the server is added. Individual tools can be enabled or disabled from this UI.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/mcp.md" target="_blank" rel="noopener noreferrer">Skill-bundled MCP servers as preferred approach</a></dt>
    <dd class="provider-extensions__description">The official recommendation is to bundle MCP servers in skills via 'mcp.json' rather than adding them to global settings. Bundled servers keep tools hidden until the skill loads, preventing tool-list bloat that can reduce model performance. Global configuration is reserved for servers that must always be available in the context window. The mcp.md source states this recommendation explicitly and links to the agent-skills documentation for the bundling mechanism.</dd>
  </div>
  <div class="provider-extensions__item">
    <dt class="provider-extensions__name"><a href="https://ampcode.com/manual/appendix/mcp-registry-allowlist.md" target="_blank" rel="noopener noreferrer">Enterprise MCP registry allowlist</a></dt>
    <dd class="provider-extensions__description">Enterprise workspace admins can enforce an MCP registry so that only approved MCP servers are available to workspace members. If the registry is unreachable, all MCP servers are blocked. The registry protocol and schema follow the standard at 'registry.modelcontextprotocol.io' (the 'v0.1/servers' endpoint). The registry URL is set in workspace settings under 'MCP Registry'. The restriction can be scoped to specific workspace member email addresses ('Apply To: Specific users only'); users not on the list are unaffected. Clearing the registry URL removes the restriction and allows all servers.</dd>
  </div>
</dl>

## Detection

Syllago detects Amp by checking for the `~/.config/amp` directory.

## Working with Amp

```bash
# Add content from Amp
syllago add --from amp

# Install content to Amp
syllago install my-rule --to amp
```

## See Also

- [Providers Overview](/using-syllago/providers/)
- [Format Conversion](/using-syllago/format-conversion/)

*Generated from syllago 0.7.0 on 2026-04-11.*
